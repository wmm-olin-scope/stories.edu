// Generated by CoffeeScript 1.6.3
(function() {
  var cityHounds, getCityHound, populateStateOption, stateList, updateCityTypeahead;

  populateStateOption = function() {
    var select, state, _i, _len, _results;
    select = $('#state-select');
    select.empty();
    _results = [];
    for (_i = 0, _len = stateList.length; _i < _len; _i++) {
      state = stateList[_i];
      _results.push(select.append("<option value=" + state + ">" + state + "</option>"));
    }
    return _results;
  };

  stateList = ['AL', 'AK', 'AZ', 'AR', 'CA', 'CO', 'CT', 'DE', 'DC', 'FL', 'GA', 'HI', 'ID', 'IL', 'IN', 'IA', 'KS', 'KY', 'LA', 'ME', 'MD', 'MA', 'MI', 'MN', 'MS', 'MO', 'MT', 'NE', 'NV', 'NH', 'NJ', 'NM', 'NY', 'NC', 'ND', 'OH', 'OK', 'OR', 'PA', 'RI', 'SC', 'SD', 'TN', 'TX', 'UT', 'VT', 'VA', 'WA', 'WV', 'WI', 'WY', 'AS', 'GU', 'MP', 'PR', 'VI'];

  cityHounds = {};

  getCityHound = function(state) {
    var hound;
    if (!(state in cityHounds)) {
      hound = new Bloodhound({
        datumTokenizer: function(d) {
          return Bloodhound.tokenizers.whitespace(d.name);
        },
        queryTokenizer: Bloodhound.tokenizers.whitespace,
        prefetch: {
          url: "/schools/cities/" + state,
          filter: function(cities) {
            var city, _i, _len, _results;
            _results = [];
            for (_i = 0, _len = cities.length; _i < _len; _i++) {
              city = cities[_i];
              _results.push({
                name: city
              });
            }
            return _results;
          }
        }
      });
      hound.initialize();
      cityHounds[state] = hound;
    }
    return cityHounds[state];
  };

  updateCityTypeahead = function() {
    var hound, state;
    state = $('#find-by-city #state-select').val();
    hound = getCityHound(state);
    return $('#find-by-city #city-input').typeahead(null, {
      name: 'cities',
      displayKey: 'name',
      source: hound.ttAdapter()
    });
  };

  $(function() {
    populateStateOption();
    return $('#find-by-city #state-select').change(updateCityTypeahead);
  });

}).call(this);
