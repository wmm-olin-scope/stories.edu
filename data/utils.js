// Generated by CoffeeScript 1.7.1
(function() {
  var mongoose;

  mongoose = require('mongoose');

  exports.makeName = function() {
    return {
      full: String,
      first: String,
      last: String
    };
  };

  exports.makeRef = function(model) {
    return {
      type: mongoose.Schema.Types.ObjectId,
      ref: model
    };
  };

  exports.makeAddress = function() {
    return {
      lines: [String],
      zip: String,
      city: String,
      state: String
    };
  };

  exports.getDisplayName = function(name) {
    switch (false) {
      case !(name == null):
        return '';
      case !name.full:
        return name.full;
      case !(name.first && name.last):
        return "" + name.first + " " + name.last;
      case !(name.first || name.last):
        return name.first || name.last;
      default:
        return '';
    }
  };

}).call(this);
